;; Functions do not exist in orena, but symbols can be used to
;; mimic them when useful, e.g. passing them as arguments to
;; higher-order symbols

let identity ≔ @x → @x

let first ≔ @x @_ → @x
let second ≔ @_ @y → @y

;; WARNING: higher-order symbols are not stable yet
let flip $f ≔ f @x @y → f @y @x
